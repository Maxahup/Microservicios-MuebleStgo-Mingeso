spring:
  application:
    name: justificativo
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
      fail-fast: true
      retry:
        max-attempts: 200
        max-interval: 10000
  datasource:
    url: jdbc:mysql://localhost:33064/muebleStgo_Justificativos
    username: root
    password: Stark021
  jpa:
    hibernate:
      ddl-auto: create
