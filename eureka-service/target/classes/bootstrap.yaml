spring:
  application:
    name: eureka-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
      fail-fast: true
      retry:
        max-attempts: 200
        max-interval: 10000